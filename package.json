{
  "name": "swag-order-server",
  "version": "0.1.0",
  "description": "server for swag order front end",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "build": "worktop build src/index.js",
    "deploy": "wrangler publish",
    "deploy:dev": "wrangler publish --env dev"
  },
  "devDependencies": {
    "worktop.build": "^0.0.5",
    "wrangler": "^2.20.0"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "esbuild-darwin-arm64": "^0.15.18",
    "faunadb": "^4.7.1",
    "worktop": "^0.7.3"
  }
}
